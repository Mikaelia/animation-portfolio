import { ProjectPage } from "../ProjectPage";
import { CodepenWrapper } from "@components/CodepenWrapper.tsx";

export const ProjectFloatingBoxesPage = () => {
  return (
    <ProjectPage
      title="Floating Boxes"
      instructions="Canvas animation of floating boxes that interact on hover"
    >
      <CodepenWrapper title="Floating Boxes" id="vYLyrOp" />
    </ProjectPage>
  );
};
