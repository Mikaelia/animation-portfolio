import { ProjectPage } from "../ProjectPage";
import { CodepenWrapper } from "@components/CodepenWrapper.tsx";

export const ProjectTextHoverUp = () => {
  return (
    <ProjectPage
      title="Text Up Hover"
      instructions="I recently stumbled across a neat hover animation for navlinks that I wanted to reproduce. Can be dried up with JS but wanted a CSS only solution."
    >
      <CodepenWrapper title="Text hover up" id="VwNzGVB" />
    </ProjectPage>
  );
};
